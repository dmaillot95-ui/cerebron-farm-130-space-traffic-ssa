import json,math,hashlib,pathlib,platform
r=(120.,-80.,40.);v=(-.12,.08,-.03);vv=sum(x*x for x in v);t=-sum(a*b for a,b in zip(r,v))/vv;t=max(0.,t);miss=math.sqrt(sum((a+b*t)**2 for a,b in zip(r,v)));ok=0<t<2000 and miss<100
out={"farm":130,"engine":"python-ssa-reference-suite-v2","test":"LINEAR_RELATIVE_MOTION_TCA","tca_s":t,"miss_distance_m":miss,"status":"REFERENCE_SUITE_OK" if ok else "FAIL","scope":"LINEAR_KINEMATIC_REFERENCE_NOT_ORBIT_COVARIANCE_OR_OPERATIONAL_SSA_VALIDATION","python":platform.python_version()};raw=json.dumps(out,sort_keys=True).encode();out["result_sha256"]=hashlib.sha256(raw).hexdigest();pathlib.Path("artifacts").mkdir(exist_ok=True);pathlib.Path("artifacts/f130_reference_suite.json").write_text(json.dumps(out,indent=2)+"\n");print(json.dumps(out));raise SystemExit(0 if ok else 1)
